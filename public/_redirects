# Old WordPress slug patterns → new routes
/blog/:slug         /journal/:slug      301
/category/:slug     /journal/category/:slug  301

# gamesasapodcast.com domain-level redirect
# Cannot be done here — Workers _redirects only allows relative URLs.
# Set this up as a Cloudflare Bulk Redirect instead (Account > Bulk Redirects in dashboard).

# Note: trailing slash normalisation is handled by Cloudflare Pages natively
